name: Sync Report Cache (all registrations)

on:
  schedule:
    # 매시간 5분에 실행 (UTC 기준). 학부모 리포트 데이터가 너무 오래 묵지 않도록 수시로 갱신합니다.
    - cron: '5 * * * *'
  workflow_dispatch: {}

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - name: Call sync-report-cache (mode=all)
        run: |
          set -e
          curl -sS -X POST "https://twczhsxybkcvjkdfdxvs.supabase.co/functions/v1/sync-report-cache" \
            -H "apikey: eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6InR3Y3poc3h5Ymtjdmprc2ZkeHZzIiwicm9sZSI6ImFub24iLCJpYXQiOjE3MDAwMDAwMDAsImV4cCI6MjAxNTU3NjAwMH0.placeholder" \
            -H "Authorization: Bearer eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6InR3Y3poc3h5Ymtjdmprc2ZkeHZzIiwicm9sZSI6ImFub24iLCJpYXQiOjE3MDAwMDAwMDAsImV4cCI6MjAxNTU3NjAwMH0.placeholder" \
            -H "x-admin-key: ${{ secrets.ADMIN_SECRET }}" \
            -H "Content-Type: application/json" \
            -d '{"mode":"all"}'
